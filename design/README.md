# Design (Figma + Lottie) — STUB

Owned by the UI/UX Designer. Placeholder folder for exported assets.

## Drop here

- Exported screens (PNG/SVG) for the Dashboard Widget + Expanded View.
- Sourced royalty-free **Lottie** JSON files for the pet, one per state:
  `idle`, `happy`, `sad`, `eating`, `sick` — matching `animationState` in
  [`../docs/DATA_MODEL.md`](../docs/DATA_MODEL.md).
- High-fidelity "fake screens" for Phase 2 (Customization Store, Squad Leaderboards).

## Naming convention (so the frontend can wire them blindly)

```
pet_idle.json   pet_happy.json   pet_sad.json   pet_eating.json   pet_sick.json
```
