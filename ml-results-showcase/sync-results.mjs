import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";const dir=path.dirname(fileURLToPath(import.meta.url));const artifacts=path.resolve(dir,"../ml-service/artifacts");const files={metrics:"metrics.json",featureImportance:"feature_importance.json",confusionMatrices:"confusion_matrices.json",recommendationExamples:"recommendation_examples.json"};const output={};for(const [key,file] of Object.entries(files)){output[key]=JSON.parse(fs.readFileSync(path.join(artifacts,file),"utf8"))}fs.writeFileSync(path.join(dir,"generated-results.js"),`window.NAMO_RESULTS = ${JSON.stringify(output,null,2)};\n`,"utf8");console.log("Synchronized generated-results.js from evaluated artifacts.");

